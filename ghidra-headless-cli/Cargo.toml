[package]
name = "ghidra-headless-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.1", features = ["derive"] }
log = "0.4.32"
simple_logger = { version = "5.2.0", default-features = false, features = ["colors", "timestamps", "stderr"] }
